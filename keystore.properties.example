# Copy this file to `keystore.properties` and fill the values.
# Keep `keystore.properties` out of version control.

# Path to the keystore file (relative to project root or absolute)
storeFile=blueprint/keystore.jks

# Keystore (store) password
storePassword=

# Alias of the key inside the keystore
keyAlias=

# Key password (if same as keystore, set the same value)
keyPassword=

# Usage notes:
# - After copying to `keystore.properties` and filling values, run Gradle normally.
# - Alternatively you can pass properties via Gradle command line:
#     ./gradlew :app:bundleRelease -PkeystoreFile=blueprint/keystore.jks -PkeystorePassword=... -PkeyAlias=... -PkeyPassword=...
# - Or set the environment variables: KEYSTORE_FILE, KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
